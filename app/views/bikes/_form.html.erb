<%= form_with(model: bike, local: true) do |form| %>
  <% if bike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bike.errors.count, "error") %> prohibited this bike from being saved:</h2>

      <ul>
      <% bike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :bike_price %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, id: :bike_model %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :bike_brand %>
  </div>

  <div class="field">
    <%= form.label :milage %>
    <%= form.number_field :milage, id: :bike_milage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
